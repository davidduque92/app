
     
          function redireccionar(op){
              var op;
              if(op==0)
              {
                  window.location='index.html';
              }else
              if(op==1)
              {
                window.location='es-se-ab.html';
              }else
              if(op==2)
              {
                window.location="es-se-to.html";
              }else
              if(op==3)
              {
                window.location="fa-tu.html";
              }else
              if(op==4)
              {
                window.location="fa-to.html";
              }else
              if(op==5)
              {
                window.location="ho-cl-to.html";
              }else
              if(op==6)
              {
                window.location="ca-ab.html";
              }else
              if(op==7)
              {
                window.location="ca-to.html";
              }else
              if(op==8)
              {
                window.location="re-ab.html";
              }else
              if(op==9)
              {
                window.location="re-to.html";
              }else
              if(op==10)
              {
                window.location="ho-ab.html";
              }else
              if(op==11)
              {
                window.location="ho-to.html";
              }else
              if(op==12)
              {
                window.location="am-co.html";
              }



          }
      
